services:
  crawler:
    build: .
    container_name: crawler_turismo_latam
    env_file:
      - .env                    # Carga las credenciales de B2
    environment:
      - PYTHONUNBUFFERED=1      # Logs en tiempo real
    restart: "no"